#include <stdio.h>
#include <signal.h>
#include <unistd.h>
#include <sys/mman.h>

int main() {
  // define name shared memory

  while(1) {
    // scan from the keybaord
    // write into name shared memory
  }
}